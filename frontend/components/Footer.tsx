export function Footer() {
  return (
    <footer className="mt-auto border-t border-black/10 px-4 py-8 text-xs text-black/45 md:px-6">
      <div className="mx-auto flex max-w-7xl flex-col gap-2 md:flex-row md:items-center md:justify-between">
        <p>Aggregated from NewsData.io, GNews, and NewsAPI.org · Bhopal / Madhya Pradesh</p>
        <p>Event tracking uses auto-generated keywords to follow progress over time.</p>
      </div>
    </footer>
  );
}
